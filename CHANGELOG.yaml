# Notice: Incompatible items start with `!`
0.2.1:
  Change:
    - Start/End with uuid
    - One step json parse
  Fix:
    - Read/Write lock ignore list now
0.2.0:
  Change:
    - ! config file add `.rb`
0.1.1:
  Feature:
    qq2matrix:
      - group text message
    matrix2qq:
      group:
        - m.text
        - m.notice
        - m.emote
